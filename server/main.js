import { Meteor } from 'meteor/meteor';

import "../imports/both/account-config.js";
//import "../imports/both/routes.js";

import "../imports/api/board/board.js";
import "../imports/api/board/publish.js";

Meteor.startup(() => {
  // code to run on server at startup
});